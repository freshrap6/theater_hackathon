export default class TimeModel {
    constructor(id,_id,day,opening,close) {
        this._id = _id
        this.id = id
        this.day = day
        this.opening = opening
        this.close = close
    }
}